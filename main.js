const body = document.querySelector('body')
const input = body.querySelector('input')


// The colour shows immediately you type in the correct colour or colour code and removes immediately after you totally remove the colour name from the input.
input.addEventListener('click', function(){return body.style.backgroundColor = input.value})


// The colour shows immediately you scroll your mouse wheel in the input field and removes immediately after you scroll your mouse wheel in the input field.
// input.addEventListener("wheel", function(){return body.style.backgroundColor = input.value})


// The colour shows immediately you click in the input field and removes immediately after you click in the input field.
// input.addEventListener('click', function(){return body.style.backgroundColor = input.value})


// The colour shows immediately you right click in the input field and removes immediately after you right click in the input field.
// input.addEventListener('contextmenu', function(){return body.style.backgroundColor = input.value})


// The colour shows immediately you double click in the input field and removes immediately after you double click in the input field.
// input.addEventListener('dblclick', function(){return body.style.backgroundColor = input.value})


// The colour shows immediately you double click in the input field and removes immediately after you double click in the input field.
// input.addEventListener('dblclick', function(){return body.style.backgroundColor = input.value})


// The colour shows immediately you click in the input field and removes immediately after you click in the input field.
// input.addEventListener('mousedown', function(){return body.style.backgroundColor = input.value})


// The colour shows immediately you move the pointer into the input field and removes immediately after you move the pointer into the input field.
// input.addEventListener('mouseenter', function(){return body.style.backgroundColor = input.value})


// The colour shows immediately you move the pointer out of the input field and removes immediately after you move the pointer out of the input field.
// input.addEventListener('mouseleave', function(){return body.style.backgroundColor = input.value})


// The colour shows immediately you move the pointer in the input field and removes immediately after you move the pointer in the input field.
// input.addEventListener('mousemove', function(){return body.style.backgroundColor = input.value})


// The colour shows immediately you move the pointer into the input field or one of its children if it had and removes immediately after you move the pointer into the input field or one of its children if it had.
// input.addEventListener('mouseover', function(){return body.style.backgroundColor = input.value})


// The colour shows immediately you mouseup in the input and removes immediately after you mouseup in the input after deleting the erasing the colour from the input field.
// input.addEventListener('mouseup', function(){return body.style.backgroundColor = input.value})


// The colour shows immediately you move your pointer out of the input field and removes immediately after you move your pointer out of the input field after deleting the erasing the colour from the input field.
// input.addEventListener('mouseout', function(){return body.style.backgroundColor = input.value})



//Test them to try them out
